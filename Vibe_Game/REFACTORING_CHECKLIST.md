# Чеклист рефакторинга проекта по SOLID принципам

## ✅ Этап 1: Подготовка (День 1)

- [ ] Установить NuGet пакет `Microsoft.Extensions.DependencyInjection`
- [ ] Создать папку `Core/Interfaces/`
- [ ] Создать папку `Core/Services/` (для реализаций)
- [ ] Создать резервную копию проекта (git commit)

## ✅ Этап 2: Создание интерфейсов (День 1-2)

- [ ] Создать `Core/Interfaces/IInputService.cs`
- [ ] Создать `Core/Interfaces/IInputBindings.cs`
- [ ] Создать `Core/Interfaces/ICamera.cs`
- [ ] Создать `Core/Interfaces/ICameraShake.cs`
- [ ] Создать `Core/Interfaces/IGameStateManager.cs`
- [ ] Создать `Core/Interfaces/IStatsRepository.cs`
- [ ] Создать `Core/Interfaces/IRandomService.cs`
- [ ] Создать `Core/Interfaces/IPlayerRenderer.cs`
- [ ] Создать `Core/Interfaces/IPlayerContentLoader.cs`
- [ ] Создать `Core/Interfaces/IItemEffect.cs`

**Проверка:** Все интерфейсы созданы, проект компилируется

## ✅ Этап 3: Рефакторинг InputManager (День 2)

- [ ] Создать `Core/Engine/InputService.cs` (реализация `IInputService`)
- [ ] Создать `Core/Engine/DefaultInputBindings.cs` (реализация `IInputBindings`)
- [ ] Удалить статический `InputManager.cs` (или оставить как обёртку для обратной совместимости)
- [ ] Обновить `PlayerController` для использования `IInputService`
- [ ] Обновить `Game1.cs` для регистрации `InputService` в DI

**Проверка:** Игрок может двигаться, проект компилируется

## ✅ Этап 4: Разделение GameManager (День 2-3)

- [ ] Создать `Core/Engine/GameStateManager.cs` (реализация `IGameStateManager`)
- [ ] Создать `Core/Engine/FileStatsRepository.cs` (реализация `IStatsRepository`)
- [ ] Создать `Core/Engine/RandomService.cs` (реализация `IRandomService`)
- [ ] Удалить или упростить `GameManager.cs` (оставить только Service Locator для миграции)
- [ ] Обновить все места использования `GameManager` на новые сервисы

**Проверка:** Состояние игры меняется, статистика сохраняется

## ✅ Этап 5: Рефакторинг Camera (День 3)

- [ ] Создать `Core/Engine/CameraShake.cs` (реализация `ICameraShake`)
- [ ] Обновить `Camera.cs` для использования `ICameraShake` через конструктор
- [ ] Реализовать `ICamera` в `Camera.cs`
- [ ] Обновить `Game1.cs` для регистрации `Camera` и `CameraShake` в DI

**Проверка:** Камера следует за игроком, тряска работает

## ✅ Этап 6: Настройка DI контейнера (День 3-4)

- [ ] Установить `Microsoft.Extensions.DependencyInjection` (если ещё не установлен)
- [ ] Создать метод `ConfigureServices()` в `Game1.cs`
- [ ] Зарегистрировать все сервисы в DI контейнере
- [ ] Создать `IServiceProvider` и сохранить в поле
- [ ] Обновить `InitializeServices()` для использования DI

**Проверка:** Все сервисы регистрируются без ошибок

## ✅ Этап 7: Рефакторинг BaseScene (День 4)

- [ ] Обновить конструктор `BaseScene` для приёма `IServiceProvider`
- [ ] Удалить методы `GetSpriteBatch()`, `GetCamera()`, `GetPixelTexture()`
- [ ] Оставить только общий `GetService<T>()`
- [ ] Обновить все наследники `BaseScene` (например, `GameScene`)

**Проверка:** Сцены работают, отрисовка корректна

## ✅ Этап 8: Рефакторинг Player (День 4-5)

- [ ] Создать `PlayerRenderer.cs` (реализация `IPlayerRenderer`)
- [ ] Создать `PlayerContentLoader.cs` (реализация `IPlayerContentLoader`)
- [ ] Обновить конструктор `Player` для приёма зависимостей
- [ ] Вынести логику отрисовки в `PlayerRenderer`
- [ ] Вынести логику загрузки в `PlayerContentLoader`
- [ ] Обновить `GameScene` для создания `Player` через DI

**Проверка:** Игрок отрисовывается и управляется корректно

## ✅ Этап 9: Система эффектов предметов (День 5-6)

- [ ] Обновить `PlayerStats` для работы с `IItemEffect`
- [ ] Создать базовые эффекты: `DamageBoostEffect`, `SpeedBoostEffect`, `HealthBoostEffect`
- [ ] Создать `CompositeItemEffect` для комбинированных эффектов
- [ ] Обновить метод `ApplyItemEffect` в `PlayerStats`
- [ ] Создать пример использования эффектов

**Проверка:** Эффекты применяются и удаляются корректно

## ✅ Этап 10: Удаление Service Locator (День 6-7)

- [ ] Найти все использования `GameManager.GetService<T>()`
- [ ] Заменить на получение через `IServiceProvider` или конструкторную инъекцию
- [ ] Удалить `GameManager.cs` или оставить только для обратной совместимости
- [ ] Удалить регистрацию сервисов в `GameManager` из `Game1.cs`

**Проверка:** Нет прямых вызовов `GameManager.GetService`

## ✅ Этап 11: Тестирование (День 7-8)

- [ ] Протестировать движение игрока
- [ ] Протестировать камеру
- [ ] Протестировать смену сцен
- [ ] Протестировать применение эффектов
- [ ] Проверить отсутствие утечек памяти
- [ ] Проверить производительность

**Проверка:** Всё работает как раньше или лучше

## ✅ Этап 12: Очистка и документация (День 8)

- [ ] Удалить неиспользуемый код
- [ ] Добавить XML комментарии к публичным API
- [ ] Обновить README с описанием архитектуры
- [ ] Создать диаграмму зависимостей (опционально)
- [ ] Финальный git commit

**Проверка:** Код чистый, документированный, проект компилируется

---

## 📝 Примечания

### Приоритет исправлений:

1. **Критично:** Этапы 1-6 (DI и интерфейсы) - основа для всего остального
2. **Важно:** Этапы 7-9 (рефакторинг сущностей) - улучшает архитектуру
3. **Желательно:** Этапы 10-12 (очистка) - финальная полировка

### Советы:

- Делайте коммиты после каждого этапа
- Тестируйте после каждого изменения
- Не пытайтесь сделать всё за один раз
- Можно делать этапы параллельно, если они не зависят друг от друга

### Обратная совместимость:

Если нужно сохранить старый код работающим во время миграции:
- Оставить старые классы как обёртки
- Постепенно переводить код на новые интерфейсы
- Удалить старый код только после полной миграции

---

## 🐛 Известные проблемы и решения

### Проблема: MonoGame Services vs DI контейнер

**Решение:** Использовать оба параллельно во время миграции:
```csharp
// Регистрируем в DI
services.AddSingleton<ICamera>(camera);

// И в MonoGame Services для обратной совместимости
Services.AddService(typeof(ICamera), camera);
```

### Проблема: ContentManager не может быть в DI

**Решение:** Передавать через параметры методов `LoadContent()` или использовать фабрику:
```csharp
services.AddSingleton<IContentManagerFactory, ContentManagerFactory>();
```

### Проблема: Game компоненты требуют Game в конструкторе

**Решение:** Использовать фабрику для создания компонентов:
```csharp
services.AddSingleton<SceneManager>(provider => 
    new SceneManager(provider.GetService<Game>(), provider));
```
